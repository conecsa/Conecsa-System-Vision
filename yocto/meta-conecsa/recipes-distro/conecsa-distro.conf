# Reference snippet from tegrademo (tegra-demo-distro) kept for historical
# context on expected DISTRO_FEATURES.
# The active distro definition is `conf/distro/conecsa.conf` (which requires
# `conf/distro/include/conecsa.inc` and sets `DISTRO = "conecsa"`).

DISTRO_FEATURES:append = " virtualization wayland opengl"
