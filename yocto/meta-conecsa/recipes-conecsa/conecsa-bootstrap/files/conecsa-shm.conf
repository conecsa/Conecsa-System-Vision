# tmpfiles.d: ensures /dev/shm exists and is mounted as tmpfs, a prerequisite
# for the POSIX segment 'conecsa_frame_shm' that the webcam-server creates and
# the inference-service consumes (ipc:shareable + ipc:"service:webcam-server").
d /dev/shm 1777 root root -
