# Websocket upgrade mapping (http context) used by the TLS server blocks to proxy
# Node-RED's /flow editor websocket and any upgrade-based streams.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
